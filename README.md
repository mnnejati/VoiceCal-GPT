# دستیار قرار ملاقات (Appointment Voice App)

اپلیکیشن اندرویدی برای ثبت قرار ملاقات با صدا (فارسی)، استخراج خودکار تاریخ/ساعت/محل/شخص، پیش‌نمایش **قابل‌ویرایش** برای تایید، ذخیره در دیتابیس محلی (Room) همراه با فایل صوتی، **یادآوری خودکار قبل از قرار**، و نمایش لیست گروه‌بندی‌شده با قابلیت پخش صدا و حذف با Undo.

## 🆕 بهبودهای نسخه‌ی فعلی نسبت به نسخه‌ی اول

- 🧠 **موتور استخراج اطلاعات هوشمندتر شد**: در حالت آنلاین حالا از یک مدل هوش مصنوعی رایگان روی Groq (`llama-3.3-70b`) برای استخراج ساختاریافته‌ی تاریخ/ساعت/محل/شخص استفاده می‌شود که مشکلات رایج زیر را حل می‌کند: تاریخ‌های نسبی مثل «دو روز دیگر»، روزهای نوشته‌شده با کلمه مثل «هفتم شهریور»، و اشتباه‌گرفتن محل با نام شخص. اگر تماس آنلاین شکست بخورد، به‌طور خودکار به موتور Regex آفلاین برمی‌گردد.
- 🐛 **رفع باگ واقعی**: تشخیص کلمه‌ی «با» به‌اشتباه داخل کلمه‌ی «بایـد» هم تطبیق پیدا می‌کرد (چون از تطبیق زیررشته استفاده می‌شد، نه مرز کلمه) و باعث می‌شد فعل جمله به‌جای نام شخص در آن فیلد قرار بگیرد. این‌بار با تطبیق مرز کلمه‌ی درست اصلاح شد.
- 🏷️ برچسب «OpenAI» در صفحه‌ی اصلی به «Groq» اصلاح شد.
- ⏱️ زمان نمایش پیام‌های Snackbar کوتاه‌تر شد (پیام‌های تاییدیه مثل «ذخیره شد» حدود ۱ ثانیه).
- ⬅️ دکمه‌ی برگشت گوشی در صفحه‌ی «قرار ملاقات‌ها» حالا به‌جای خروج از اپ، به صفحه‌ی اصلی برمی‌گردد.
- 📐 نمایش ساعت در لیست قرار ملاقات‌ها به یک سطر مجزا و همیشه افقی زیر تاریخ منتقل شد (رفع مشکل نوشتار عمودی روی کارت‌های باریک).
- 🔔 **آلارم قابل‌تنظیم**: از تنظیمات می‌توانید آهنگ دلخواه برای یادآوری انتخاب کنید و مدت‌زمان پخش آن (۳ تا ۱۲۰ ثانیه) را تعیین کنید؛ هنگام یادآوری، صدا پخش می‌شود و یک نوتیفیکیشن با دکمه‌ی «توقف» نمایش داده می‌شود.

- 💰 **مدل آنلاین رایگان**: به‌جای OpenAI Whisper (پولی)، از **Groq (Whisper Large v3)** استفاده می‌شود — کاملاً رایگان، بدون نیاز به کارت بانکی، حدود ۲۰۰۰ درخواست رایگان در روز.
- 🏷️ **متن دکمه** به «Start Talk» / «Recording... Release» تغییر کرد.
- ✏️ **قابلیت ویرایش** به لیست قرار ملاقات‌ها اضافه شد: با زدن آیکون مداد ✏ کنار هر ردیف، می‌توانید تاریخ/ساعت/محل/شخص را اصلاح کنید (بدون نیاز به ضبط دوباره‌ی صدا).
- 🛠️ **رفع باگ بارگذاری مدل آفلاین**: روش صحیح قرار دادن مدل Vosk (پوشه‌ی خام، نه zip دستی) اصلاح شد و یک Gradle task فایل ضروری `uuid` را خودکار می‌سازد. توضیح کامل در بخش «راه‌اندازی حالت آفلاین» پایین‌تر.

- 🔥 **معماری صداپردازی کاملاً بازطراحی شد (رفع قطعی مشکل «سکوت»)**: حالا ابتدا صدا فقط ضبط و در حافظه نگه داشته می‌شود؛ **بعد از رهاکردن دکمه**، فایل صوتی کامل به‌صورت یک مرحله‌ی جدا برای تشخیص گفتار فرستاده می‌شود. دیگر هیچ دو مصرف‌کننده‌ی هم‌زمانِ میکروفون وجود ندارد، پس مشکل «سکوت»/«صدایی شنیده نشد» که با ضبط + تشخیص هم‌زمان پیش می‌آمد، از ریشه رفع شد.
- ⚙️ **دو موتور تشخیص گفتار قابل‌انتخاب از تنظیمات** (آیکون ⚙ بالای صفحه‌ی اصلی):
  - **آنلاین (OpenAI Whisper)** — پیش‌فرض؛ دقت بالا و سرعت خوب، نیاز به اینترنت (و شاید فیلترشکن بسته به کشور) و یک کلید API از OpenAI دارد.
  - **آفلاین (Vosk)** — بدون نیاز به اینترنت، اما نیاز به دانلود دستی یک فایل مدل فارسی (~۵۰ مگابایت) دارد؛ دقتش کمی پایین‌تر از حالت آنلاین است.

- ✅ **یادآوری/نوتیفیکیشن**: ۳۰ دقیقه قبل از هر قرار، نوتیفیکیشن یادآوری نمایش داده می‌شود (`AlarmManager` + `BroadcastReceiver`)، حتی بعد از ریستارت گوشی (`BootReceiver` دوباره زمان‌بندی می‌کند).
- ✅ **پیش‌نمایش قابل‌ویرایش**: قبل از ذخیره، تاریخ/ساعت/محل/شخص در فیلدهای قابل‌ویرایش نمایش داده می‌شوند تا اگر تشخیص گفتار اشتباه کرد، کاربر خودش اصلاح کند.
- ✅ **محاسبه‌ی مطمئن روز هفته**: به‌جای تکیه بر گفتار، روز هفته همیشه از خودِ تاریخ محاسبه می‌شود (هرگز نمی‌تواند با تاریخ ناسازگار باشد).
- ✅ **بازخورد خطا**: اگر صدایی تشخیص داده نشود یا چیز قابل‌فهمی استخراج نشود، پیام مناسب (Snackbar) به کاربر نمایش داده می‌شود؛ دیگر چیزی بی‌صدا گم نمی‌شود.
- ✅ **حذف با Undo**: حذف هر قرار فوری و بدون دیالوگ تاییدِ اضافه انجام می‌شود، اما یک Snackbar با دکمه‌ی «واگرد» ۴ ثانیه فرصت بازگردانی می‌دهد؛ فایل صوتی و ردیف دیتابیس فقط بعد از اتمام این فرصت واقعاً حذف می‌شوند.
- ✅ **گروه‌بندی لیست**: قرارها زیر سربرگ‌های «امروز»، «فردا»، «این هفته»، «به‌زودی» و «تاریخ نامشخص» دسته‌بندی می‌شوند.
- ✅ **پشتیبانی از حالت تیره (Dark Mode)** بر اساس تنظیمات سیستم.
- ✅ **ایمنی چرخه‌حیات**: میکروفون/Recognizer/Player در `onCleared` همیشه آزاد می‌شوند؛ دیگر نشتی منبع در چرخش صفحه یا خروج از اپ رخ نمی‌دهد.
- ✅ **تست واحد** برای موتور استخراج فارسی (`PersianInfoExtractorTest`).

## ☁️ ساخت APK بدون نصب Android Studio (رایگان، با GitHub Actions)

اگر نمی‌خواهید Android Studio نصب کنید، می‌توانید از **GitHub Actions** استفاده کنید — سرویس ابری رایگان گیت‌هاب که به‌جای شما پروژه را کامپایل می‌کند. فایل تنظیمات لازم (`.github/workflows/build.yml`) از قبل داخل این پروژه آماده شده. مراحل:

1. یک حساب رایگان در [github.com](https://github.com) بسازید (اگر ندارید).
2. یک ریپازیتوری جدید بسازید (روی دکمه‌ی سبز **New**؛ می‌توانید Public بگذارید تا محدودیت زمانی نداشته باشید).
3. داخل صفحه‌ی ریپازیتوری، روی «uploading an existing file» کلیک کنید و **کل محتوای پوشه‌ی پروژه (`VoiceCal` یا هر اسمی که به آن داده‌اید)** (بعد از اکسترکت کردن zip) را با درگ‌ودراپ در مرورگر بارگذاری کنید (گیت‌هاب ساختار پوشه‌ها را هم حفظ می‌کند).
4. روی **Commit changes** بزنید.
5. به تب **Actions** بروید — بیلد به‌طور خودکار شروع می‌شود (چند دقیقه طول می‌کشد).
6. وقتی بیلد سبز شد، روی آن اجرا (run) کلیک کنید، در پایین صفحه بخش **Artifacts** را پیدا کنید و فایل `appointment-voice-app-debug-apk` را دانلود کنید — یک zip حاوی `app-debug.apk` است.

نکات:
- این روش کاملاً رایگان است (گیت‌هاب برای ریپازیتوری‌های Public دقیقه‌ی بیلد نامحدود می‌دهد).
- APK تولیدشده نسخه‌ی **debug** است (بدون امضای انتشار)؛ برای نصب مستقیم روی گوشی خودتان کافی است، اما برای انتشار در Google Play نیاز به امضای release دارد.
- اگر git را بلد باشید، به‌جای آپلود دستی می‌توانید با `git init`, `git add .`, `git commit`, `git push` هم همین کار را انجام دهید.
- گزینه‌های مشابه دیگر (در صورت تمایل به امتحان جایگزین): [Codemagic](https://codemagic.io) و [Bitrise](https://bitrise.io) هم پلن رایگان برای بیلد اندروید دارند، ولی برای این پروژه‌ی ساده، GitHub Actions ساده‌ترین گزینه است.

## 🌐 راه‌اندازی حالت آنلاین (پیش‌فرض، توصیه‌شده — رایگان)

1. به [console.groq.com](https://console.groq.com) بروید و با ایمیل یا حساب گوگل ثبت‌نام کنید (بدون نیاز به کارت بانکی).
2. از صفحه‌ی [console.groq.com/keys](https://console.groq.com/keys) یک کلید API جدید بسازید (با `gsk_` شروع می‌شود).
3. داخل اپ، روی آیکون ⚙ بالای صفحه‌ی اصلی بزنید، حالت **«آنلاین (Groq — رایگان)»** را انتخاب کنید، کلید را در فیلد مربوطه وارد و **ذخیره** کنید.
4. همین! کلید فقط به‌صورت محلی روی گوشی خودتان ذخیره می‌شود (هیچ‌جای دیگری فرستاده نمی‌شود). Groq در پلن رایگان روزانه حدود ۲۰۰۰ درخواست تشخیص گفتار اجازه می‌دهد که برای استفاده‌ی شخصی کاملاً کافی است.

اگر سرویس‌های Groq در شبکه‌ی شما فیلتر است، فعال‌کردن فیلترشکن هنگام ارسال صدا (بعد از رهاکردن دکمه) کافی است — چون این‌بار ضبط و ارسال دو مرحله‌ی جدا هستند، مشکل «سکوت» قبلی دیگر تکرار نمی‌شود.

## 🎤 راه‌اندازی حالت آفلاین (اختیاری — فقط اگر بخواهید بدون اینترنت کار کند)

اگر می‌خواهید بدون هیچ اینترنتی کار کند (دقت کمی پایین‌تر)، از تنظیمات حالت **«آفلاین (Vosk)»** را انتخاب کنید. این حالت نیاز به فایل‌های مدل فارسی دارد که باید یک‌بار خودتان اضافه کنید (چون حدود ۵۰ مگابایت است و در کد قرار نگرفته):

**⚠️ نکته‌ی مهم که نسخه‌ی قبلی اشتباه بود:** باید **پوشه‌ی خامِ اکسترکت‌شده** را در assets قرار دهید، نه یک فایل zip دستی — کتابخانه‌ی Vosk به این شکل انتظار دارد، و همین اشتباه باعث خطای «موتور تشخیص گفتار بارگذاری نشد» در نسخه‌ی قبلی می‌شد.

1. به آدرس [alphacephei.com/vosk/models](https://alphacephei.com/vosk/models) بروید.
2. در بخش **Farsi**، فایل **`vosk-model-small-fa-0.42.zip`** (حدود ۵۳ مگابایت) را دانلود کنید.
3. این فایل را **اکسترکت/باز کنید** (Extract/Unzip) — یک پوشه به‌نام چیزی شبیه `vosk-model-small-fa-0.42` ساخته می‌شود که داخلش زیرپوشه‌هایی مثل `am`, `conf`, `graph`, `ivector` و... هست.
4. این پوشه را (خودِ پوشه، نه محتویاتش تنها) به نام دقیق **`model-fa-fa`** تغییر نام دهید.
5. کل این پوشه (با تمام زیرپوشه‌ها و فایل‌هایش) را در مسیر زیر از پروژه کپی کنید، طوری که ساختار نهایی این‌طور باشد:
   ```
   app/src/main/assets/model-fa-fa/am/...
   app/src/main/assets/model-fa-fa/conf/...
   app/src/main/assets/model-fa-fa/graph/...
   app/src/main/assets/model-fa-fa/ivector/...
   ```
6. در گیت‌هاب آپلود کنید. چون این پوشه شامل چند ده فایل کوچک است (نه یک فایل تکی بزرگ)، آپلود درگ‌ودراپ معمولی معمولاً برایش کار می‌کند؛ اگر مشکلی داشت، از طریق نصب Git روی سیستم خودتان و دستورات `git add`, `git commit`, `git push` آپلود کنید (اگر با git راحت نیستید بگویید تا مرحله‌به‌مرحله راهنمایی کنم).
7. Commit بزنید. یک Gradle task در پروژه (`ensureVoskModelUuid`) به‌طور خودکار در زمان بیلد یک فایل ضروری به‌نام `uuid` داخل این پوشه می‌سازد (چون مدل‌های دانلودی رسمی این فایل را ندارند و کتابخانه‌ی Vosk بدون آن خطا می‌دهد) — نیازی به کار دستی برای این مرحله نیست.
8. صبر کنید بیلد اجرا شود، APK جدید را نصب کنید.

**نکته‌ی مهم:** اگر فقط از حالت آنلاین (پیش‌فرض) استفاده می‌کنید، اصلاً نیازی به این پوشه‌ی مدل نیست — می‌توانید این بخش را کلاً نادیده بگیرید.

## ⚠️ نکته مهم درباره‌ی ساخت APK


این پروژه به صورت **کد کامل و آماده** تحویل داده شده، اما در محیطی که این پاسخ تولید شده، دسترسی به مخازن Google Maven (`dl.google.com`) که برای دانلود Android SDK / Android Gradle Plugin لازم است وجود ندارد؛ بنابراین امکان بیلد گرفتن APK در همین‌جا نبود. برای گرفتن فایل APK باید پروژه را روی سیستم خودتان بیلد کنید — کار سختی نیست، مراحل زیر را دنبال کنید.

## مراحل ساخت APK

1. [Android Studio](https://developer.android.com/studio) (نسخه‌ی Jellyfish یا جدیدتر) را نصب کنید.
2. پوشه‌ی پروژه (`VoiceCal` یا هر اسمی که به آن داده‌اید) را با گزینه‌ی **Open** در Android Studio باز کنید (Android Studio به‌طور خودکار فایل‌های Gradle Wrapper لازم را می‌سازد).
3. صبر کنید تا Gradle Sync کامل شود (نیاز به اینترنت برای دانلود وابستگی‌ها دارد).
4. از منو: `Build > Build Bundle(s) / APK(s) > Build APK(s)`.
5. فایل خروجی در مسیر زیر قرار می‌گیرد:
   `app/build/outputs/apk/debug/app-debug.apk`

یا از طریق ترمینال (بعد از باز کردن یک‌بار در Android Studio تا wrapper ساخته شود):
```
./gradlew assembleDebug
```

## معماری و ویژگی‌ها

- **Jetpack Compose** برای رابط کاربری (طراحی گرادیانی بنفش/سرمه‌ای، انیمیشن پالس میکروفون).
- **دکمه "Start Talk"**: با نگه‌داشتن (press & hold) ضبط شروع و با رهاکردن انگشت متوقف و پردازش می‌شود.
  - `VoiceCaptureEngine` فقط و فقط صدا را ضبط می‌کند (`AudioRecord`، ۱۶kHz مونو) و در حافظه نگه می‌دارد تا رهاشدن دکمه — هیچ تشخیص گفتاری هم‌زمان با ضبط انجام نمی‌شود.
  - بعد از رهاشدن دکمه، فایل WAV نوشته می‌شود و به `TranscriptionEngine` انتخاب‌شده (`GroqWhisperTranscriber` آنلاین یا `OfflineVoskTranscriber` آفلاین) به‌صورت یک مرحله‌ی جدا و ترتیبی سپرده می‌شود — چون ضبط و تشخیص هرگز هم‌زمان نیستند، مشکل تداخل میکروفون از ریشه رفع شده.
  - `SettingsStore` انتخاب حالت (آنلاین/آفلاین) و کلید API را به‌صورت محلی (SharedPreferences) ذخیره می‌کند؛ از طریق آیکون ⚙ در صفحه‌ی اصلی (`SettingsDialog`) قابل تغییر است.
  - تمام پردازش گفتار کاملاً **آفلاین** است — نه اینترنت لازم است، نه فیلترشکن، نه وابستگی به اپ گوگل.
- **`PersianInfoExtractor`**: با قواعد و عبارت‌های باقاعده (regex) تاریخ شمسی (امروز/فردا/پس‌فردا، نام روز هفته، نام ماه شمسی، تاریخ عددی)، ساعت (عددی یا با کلمه، صبح/عصر/شب)، محل (بعد از "در")، و شخص (بعد از "با") را استخراج می‌کند. تمام موارد اختیاری‌اند — هرکدام که در گفتار نبود، خالی می‌ماند.
- **`PersianCalendar`**: تبدیل تاریخ شمسی↔میلادی برای محاسبه‌ی کلید مرتب‌سازی (نزدیک‌ترین تاریخ اول).
- **Room Database**: جدول `appointments` شامل تمام فیلدهای استخراج‌شده + مسیر فایل صوتی.
- **صفحه‌ی پیش‌نمایش (قابل‌ویرایش)**: بعد از پردازش صدا، فیلدهای تاریخ/ساعت/محل/شخص در قالب فرم قابل‌اصلاح نمایش داده می‌شوند؛ کاربر می‌تواند قبل از «تایید و ذخیره» هرکدام را دستی درست کند.
- **`ReminderScheduler` / `NotificationHelper` / `ReminderBroadcastReceiver` / `BootReceiver`**: ۳۰ دقیقه قبل از هر قرار (در صورت مشخص بودن ساعت) نوتیفیکیشن یادآوری زمان‌بندی می‌شود؛ با حذف قرار لغو و با ریستارت گوشی دوباره زمان‌بندی می‌شود.
- **صفحه‌ی "نمایش قرار ملاقات‌ها"**: لیست کارت‌مانند، گروه‌بندی‌شده زیر سربرگ‌های «امروز/فردا/این هفته/به‌زودی»، مرتب‌شده از نزدیک‌ترین به دورترین تاریخ؛ هر ردیف شامل:
  - دکمه‌ی دایره‌ای مثلثی (▶) برای پخش صدا که با کلیک به دکمه‌ی توقف (■) تبدیل می‌شود.
  - تاریخ، روز هفته (محاسبه‌شده از خودِ تاریخ، نه صرفاً گفتار)، ساعت، محل و شخص.
  - دکمه‌ی حذف (🗑) که فوری حذف می‌کند اما ۴ ثانیه فرصت «واگرد» (Snackbar) می‌دهد؛ حذف واقعی ردیف دیتابیس و فایل صوتی فقط بعد از اتمام این فرصت انجام می‌شود.

## دسترسی‌های لازم

- `RECORD_AUDIO`: برای ضبط صدا (در زمان اجرا هم درخواست می‌شود).
- `POST_NOTIFICATIONS` (اندروید ۱۳ به بالا): برای نمایش نوتیفیکیشن یادآوری (در زمان اجرا درخواست می‌شود؛ در صورت رد شدن، اپ کاملاً کار می‌کند فقط یادآوری نمایش داده نمی‌شود).
- `RECEIVE_BOOT_COMPLETED`: برای زمان‌بندی دوباره‌ی یادآوری‌ها بعد از ریستارت گوشی (چون `AlarmManager` با ریبوت پاک می‌شود).
- فایل‌های صوتی در حافظه‌ی خصوصی اپ ذخیره می‌شوند، پس نیازی به دسترسی حافظه‌ی عمومی نیست.

## توسعه‌های پیشنهادی بعدی (اولویت‌بندی‌شده)

- **ویرایش قرار موجود** (نه فقط حذف و ثبت دوباره) — افزودن `updateAppointment` در DAO + صفحه‌ی ویرایش.
- **جستجو/فیلتر** در لیست قرارها (بر اساس شخص، محل، یا بازه‌ی تاریخ).
- **لغو ضبط با کشیدن انگشت به بالا** (الگوی رایج پیام صوتی واتس‌اپ/تلگرام) به‌جای فقط رهاکردن ساده.
- **موج‌نمای صوتی زنده (waveform)** به‌جای دایره‌ی پالسی ساده، برای بازخورد بصری دقیق‌تر حین ضبط.
- **یادآوری دقیق‌تر**: در حال حاضر از `setAndAllowWhileIdle` (غیردقیق، با تاخیر چند دقیقه‌ای مجاز) استفاده شده تا نیازی به مجوز ویژه‌ی `SCHEDULE_EXACT_ALARM` نباشد. اگر دقت ثانیه‌ای لازم شد، باید این مجوز را اضافه و از کاربر گرفت.
- **پشتیبان‌گیری/بازیابی** دیتابیس و فایل‌های صوتی (خروجی zip یا اتصال به فضای ابری کاربر).
- **رمزنگاری فایل‌های صوتی حساس** در صورت نیاز به حریم خصوصی بالاتر (`EncryptedFile` از Jetpack Security).
- **دسترسی‌پذیری کامل (Accessibility)**: تست کامل با TalkBack، برچسب‌های محتوایی برای همه‌ی دکمه‌ها.
- **ویجت صفحه‌ی اصلی** برای نمایش نزدیک‌ترین قرار ملاقات بدون باز کردن اپ.
- **پشتیبانی چندزبانه** (در کنار فارسی) در صورت نیاز به بازار بین‌المللی.
- پشتیبانی از تشخیص افست‌های نسبی بیشتر ("هفته‌ی دیگه"، "دوشنبه‌ی هفته‌ی بعد").
